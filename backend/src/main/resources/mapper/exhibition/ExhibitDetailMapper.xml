<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.feiyi.module.exhibition.dao.ExhibitDetailDao">

    <!-- 根据展品ID获取详情 -->
    <select id="getByExhibitId" resultType="com.feiyi.module.exhibition.domain.ExhibitDetailEntity">
        SELECT id, exhibit_id AS exhibitId, category, era, origin, material, size,
               history, craft, cultural_value AS culturalValue, audio_path AS audioPath, video_path AS videoPath,
               create_time AS createTime, update_time AS updateTime
        FROM t_exhibit_detail
        WHERE exhibit_id = #{exhibitId}
    </select>

</mapper>
